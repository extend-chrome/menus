import { Subscription } from 'rxjs'
/**
 * Map of basic context menu ids to selector context menu id arrays
 */
export declare const optionsMap: Map<
  string,
  [ContextMenuOptions, Subscription | null]
>
